from setuptools import setup, find_packages

setup(
    name='multimodal_classification',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # already mentioned in requirements.txt
    ],
)
